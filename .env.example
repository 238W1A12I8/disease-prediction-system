# ============================================
# HealthPredict Production Environment Variables
# ============================================
# Copy this file to .env and fill in the values

# ---- JWT Security (REQUIRED) ----
# Generate a strong 32+ character Base64 encoded secret
# Example: openssl rand -base64 32
APP_JWT_SECRET=your-super-secret-jwt-key-here-min-32-chars

# JWT token expiration in milliseconds (default: 24 hours)
APP_JWT_EXPIRATION=86400000

# ---- Database Configuration ----
# For H2 (development) - no configuration needed
# For PostgreSQL (production):
DB_HOST=localhost
DB_PORT=5432
DB_NAME=healthpredict
DB_USERNAME=healthpredict
DB_PASSWORD=your-database-password

# ---- Server Configuration ----
SERVER_PORT=8082

# ---- Spring Profile ----
# Use 'prod' for production with PostgreSQL
SPRING_PROFILES_ACTIVE=default

# ---- Docker Hub (for CI/CD) ----
# Only needed if pushing to Docker Hub
DOCKERHUB_USERNAME=your-dockerhub-username
DOCKERHUB_TOKEN=your-dockerhub-token

# ============================================
# Usage Examples
# ============================================
#
# Development (H2 in-memory):
#   ./mvnw spring-boot:run
#
# Production (PostgreSQL):
#   export $(cat .env | xargs) && ./mvnw spring-boot:run -Dspring.profiles.active=prod
#
# Docker:
#   docker-compose up --build
#
